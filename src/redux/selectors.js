// Rockets Selcectors
export const rocketsDataStageSelector = (state) => state.rocketsData.stage
export const rocketsDataSelector = (state) => state.rocketsData.rockets

// Rocket Item Selcectors
export const rocketItemStageSelector = (state) => state.rocketItemData.stage
export const rocketItemSelector = (state) => state.rocketItemData.rocket
